# Belatime

This project was created for Belatrix selection process.

### Requirements

- Node Js
- NPM

### Run project

The base project is a react-create-app application.
To run the project it is needed to type `npm start`.
To run the test it is needed to type  `npm test`
